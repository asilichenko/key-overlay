# Key Overlay

Display pressed keys in the overlay:

- LCtrl
- RCtrl
- LShift
- RShift

Requirements:

```
pip install -r requirements.txt
```

Run:

```
python -m key_overlay
```

Build exe:

```
python scripts/build.py
```